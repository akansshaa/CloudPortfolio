# CloudPortfolio
My weather app uses a cloud-based request–response algorithm. The GUI sends an HTTP GET request to a cloud API, which returns JSON data containing temperature and wind speed. I then parse that JSON and display it in the GUI.
